source("functions.R")
# formからのデータをよみだす
form_data = read.csv("csv/JNNS2024_flanker_answer.csv", header = TRUE, sep = ",")
# ID(ファイル名)を取得
ids = form_data[, 7]
# 分析用dfを作っておく
df = data.frame(
displayFormat = character(), # 表示形式
incongruency = character(), # 一致不一致
RT_ave = numeric() # RT
)
View(df)
# file_names = c("controll", "2D", "barrier", "Far")
file_names = c("controll")
ids = c("AHA2")
# パスつくる
path = paste0("csv/", id, "/")
# csv読み出して確認
response_data = read.csv(paste0(path, id, file_name,".csv"), header = TRUE, sep = ",")
View(response_data)
source("functions.R")
# formからのデータをよみだす
form_data = read.csv("csv/JNNS2024_flanker_answer.csv", header = TRUE, sep = ",")
# ID(ファイル名)を取得
ids = form_data[, 7]
# 分析用dfを作っておく
df = data.frame(
displayFormat = character(), # 表示形式
incongruency = character(), # 一致不一致
RT_ave = numeric() # RT
)
View(df)
# file_names = c("controll", "2D", "barrier", "Far")
file_name = "controll"
id = "AHA2"
# パスつくる
path = paste0("csv/", id, "/")
# csv読み出して確認
response_data = read.csv(paste0(path, id, file_name,".csv"), header = TRUE, sep = ",")
View(response_data)
congruent_data = choose_by_incongruency(response_data, "congruent")
congruent_data = chose_by_incongruency(response_data, "congruent")
congruent_data = remove_unnecesary_data(congruent_data)
congruent_data = remove_unnecessary_data(congruent_data)
source("functions.R")
# formからのデータをよみだす
form_data = read.csv("csv/JNNS2024_flanker_answer.csv", header = TRUE, sep = ",")
# ID(ファイル名)を取得
ids = form_data[, 7]
# 分析用dfを作っておく
df = data.frame(
displayFormat = character(), # 表示形式
incongruency = character(), # 一致不一致
RT_ave = numeric() # RT
)
View(df)
# file_names = c("controll", "2D", "barrier", "Far")
file_name = "controll"
id = "AHA2"
# パスつくる
path = paste0("csv/", id, "/")
# csv読み出して確認
response_data = read.csv(paste0(path, id, file_name,".csv"), header = TRUE, sep = ",")
View(response_data)
congruent_data = chose_by_incongruency(response_data, "congruent")
congruent_data = remove_unnecessary_data(congruent_data)
source("functions.R")
# formからのデータをよみだす
form_data = read.csv("csv/JNNS2024_flanker_answer.csv", header = TRUE, sep = ",")
# ID(ファイル名)を取得
ids = form_data[, 7]
# 分析用dfを作っておく
df = data.frame(
displayFormat = character(), # 表示形式
incongruency = character(), # 一致不一致
RT_ave = numeric() # RT
)
View(df)
# file_names = c("controll", "2D", "barrier", "Far")
file_name = "controll"
id = "AHA2"
# パスつくる
path = paste0("csv/", id, "/")
# csv読み出して確認
response_data = read.csv(paste0(path, id, file_name,".csv"), header = TRUE, sep = ",")
View(response_data)
congruent_data = chose_by_incongruency(response_data, "congruent")
filtered_data = remove_unnecessary_data(congruent_data)
source("functions.R")
# formからのデータをよみだす
form_data = read.csv("csv/JNNS2024_flanker_answer.csv", header = TRUE, sep = ",")
# ID(ファイル名)を取得
ids = form_data[, 7]
# 分析用dfを作っておく
df = data.frame(
displayFormat = character(), # 表示形式
incongruency = character(), # 一致不一致
RT_ave = numeric() # RT
)
# file_names = c("controll", "2D", "barrier", "Far")
file_name = "controll"
# display_formats = c("VR Object", "2D Object", "VR Object with barrier", "VR Object with distance)
display_format = "VR Object"
id = "AHA2"
# パスつくる
path = paste0("csv/", id, "/")
# csv読み出して確認
response_data = read.csv(paste0(path, id, file_name,".csv"), header = TRUE, sep = ",")
View(response_data)
# 要因ごとに処理
congruent_data = chose_by_incongruency(response_data, "congruent")
filtered_congruent = remove_unnecessary_data(congruent_data)
# View(filtered_data)
congruent_ave = mean(filtered_congruent$RT, na.rm = TRUE)
df = add_row(df, display_format, "congruent", congruent_ave)
View(df)
